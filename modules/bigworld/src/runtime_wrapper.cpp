#include "apollo/bigworld/runtime.hpp"

// This file provides the bridge between apollo::bw and apollo::bigworld
// The implementation is in apollo::bw namespace

namespace apollo::bigworld {
    // All functions are inlined in the header, using apollo::bw implementations
}
